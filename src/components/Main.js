import React from "react";
import SideBar from "./SideBar";
import Viewport from "./Viewport";

const Main = () => {
  return (
    <div id="main">
      <SideBar />
      <Viewport />
    </div>
  );
};

export default Main;
